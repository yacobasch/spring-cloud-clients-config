---
spring:
  profiles: prod
logging:
  level:
    org.springframework.boot: DEBUG
env2: production
---
spring:
  profiles: default
logging:
  level:
    org.springframework.boot: DEBUG
env1: default
